import { CSSObject } from "@mantine/core";

export const durationContainer: CSSObject = {
  backgroundColor: "#fff",
  padding: "1em",
  textAlign: "center",
};

export const textCursor: CSSObject = {
  cursor: "text",
};
